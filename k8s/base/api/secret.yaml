# DEV-ONLY placeholder credentials — same caveat as postgres/secret.yaml.
apiVersion: v1
kind: Secret
metadata:
  name: api-secret
  namespace: employee-management
type: Opaque
stringData:
  DB_USERNAME: postgres
  DB_PASSWORD: postgres-dev-only-change-me
  JWT_SECRET: dev-only-jwt-secret-change-in-production-min-256-bits-long
