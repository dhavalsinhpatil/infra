# prod environment

Not yet built. Will mirror `environments/dev` once the dev environment has
been applied and validated — same modules, separate `backend.tf` key
(`prod/terraform.tfstate`), tighter resource sizing/variables.
