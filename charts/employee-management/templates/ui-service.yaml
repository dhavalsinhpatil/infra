apiVersion: v1
kind: Service
metadata:
  name: ui
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: ui
  ports:
    - port: {{ .Values.ui.service.port }}
      targetPort: 80
      name: http
